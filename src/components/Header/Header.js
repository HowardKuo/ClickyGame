import React from "react";
import "./Header.css";

const Header = () => (
	<header className = "header">
		<h1>Zodiac Clicky Game!</h1>
		<h2>Click on as many different animals if you can! If you click the same animal twice, you will lose. Get all 12 to win!</h2>
	</header>
);

export default Header;